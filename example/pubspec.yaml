name: flutter_popup_menu_example
description: A Flutter implementation of animation popup menu.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  
  popup_menu:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.17.10

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
